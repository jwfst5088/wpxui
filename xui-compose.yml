version: "3.9"
services:
  xui:
    image: enwaiax/x-ui
    container_name: xui
    restart: always
    network_mode: host
    volumes:
        - "$PWD/out:/root/out"
        - "$PWD/generate_cert.sh:/generate_cert.sh"  
    command: ["/bin/bash", "/generate_cert.sh"]


